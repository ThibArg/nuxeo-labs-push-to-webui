<?xml version="1.0"?>
<fragment version="1">

  <require>org.nuxeo.web.ui</require>

  <!-- Register the PushServlet -->
  <extension target="web#SERVLET">
    <servlet>
      <servlet-name>Nuxeo Push Notification Servlet</servlet-name>
      <servlet-class>nuxeo.labs.push.PushServlet</servlet-class>
      <async-supported>true</async-supported>
    </servlet>
    <servlet-mapping>
      <servlet-name>Nuxeo Push Notification Servlet</servlet-name>
      <url-pattern>/push/subscribe</url-pattern>
    </servlet-mapping>
  </extension>

  <!-- Wire Nuxeo authentication filter to the push endpoint -->
  <extension target="web#STD-AUTH-FILTER">
    <filter-mapping>
      <filter-name>NuxeoAuthenticationFilter</filter-name>
      <url-pattern>/push/subscribe</url-pattern>
    </filter-mapping>
  </extension>

  <install>
    <!-- Install web resources, excluding i18n (handled separately) -->
    <unzip from="${bundle.fileName}" to="/" prefix="web">
      <include>web/nuxeo.war/**</include>
      <exclude>web/nuxeo.war/ui/i18n/**</exclude>
    </unzip>

    <!-- Handle i18n: append translations -->
    <delete path="${bundle.fileName}.tmp"/>
    <mkdir path="${bundle.fileName}.tmp"/>
    <unzip from="${bundle.fileName}" to="${bundle.fileName}.tmp">
      <include>web/nuxeo.war/ui/i18n/**</include>
    </unzip>

    <append from="${bundle.fileName}.tmp/web/nuxeo.war/ui/i18n/messages.json" to="nuxeo.war/ui/i18n/messages.json" addNewLine="true"/>

    <delete path="${bundle.fileName}.tmp"/>
  </install>

</fragment>
